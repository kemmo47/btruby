<nav class="navbar navbar-expand-lg navbar-light">
    <div class="container-fluid">
        <b>
            <% if content_for(:title) %>
                <%= yield :title %>
            <% else %>
                Foodlog
            <% end %>
        </b>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <% if user_signed_in? %>
                <li class="nav-item">
                    <%= link_to 'Home', root_path, class: "nav-link active ms-5" %>
                </li>
                <li class="nav-item">
                    <%= link_to 'Foodlog', entries_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                    <%= link_to 'Add Foodlog', new_entry_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                    <%= link_to 'Todos', todos_path, class: "nav-link" %>
                </li>
                <% end %>
            </ul> 
            <ul class="nav align-items-center">
                <% if user_signed_in? %>
                    <li class="nav-item">
                        <%= link_to edit_user_registration_path do %>
                            <div class="d-flex align-items-center">
                                <input type="hidden" value="<%= current_user.email %>" class="avatar_user">
                                <div class="img_avatar"></div>
                                <div class="nav-link text-success">Edit Profile</div>
                            </div>
                        <% end %>
                    </li>
                    <li class="nav-item">
                        <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: "nav-link text-danger" %>
                    </li>
                <% else %>
                    <li class="nav-item">
                        <%= link_to 'Sign Up', new_user_registration_path, class: "nav-link" %>
                    </li>
                    <li class="nav-item">
                        <%= link_to 'Sign In', new_user_session_path, class: "nav-link" %>
                    </li>
                <% end %>
            </ul>
            
        </div>
    </div>
</nav>